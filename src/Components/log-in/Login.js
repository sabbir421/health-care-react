import React from 'react';
import '../log-in/Login.css'

const Login = () => {
    return (
        <div>
            <button onClick='' className=" btn">sing in with google</button>
        </div>
    );
};

export default Login;